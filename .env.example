# Copy to .env.local and replace with values from Firebase Console → Project settings → Your apps.
# NEXT_PUBLIC_* variables are exposed to the browser; protect data with Firestore/Storage rules + Auth.

NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef123456
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Canonical URL for OG tags, sitemap, robots, and product QR deep links (no trailing slash)
# Example: https://secondskinmensworld.com — printed QRs use this for Scan & stock ?product=…
NEXT_PUBLIC_SITE_URL=https://secondskinmensworld.com

# Shiprocket (server-only — never prefix with NEXT_PUBLIC_)
# Used by /api/shiprocket/create-order to auto-book a shipment when a customer places an order.
SHIPROCKET_EMAIL=you@example.com
SHIPROCKET_PASSWORD=your-shiprocket-password
# Nickname of the pickup location registered in Shiprocket (Settings → Pickup Addresses)
SHIPROCKET_PICKUP_LOCATION=Primary
# Default package dimensions (cm) and per-item weight (kg) for the adhoc order payload
SHIPROCKET_PACKAGE_LENGTH_CM=25
SHIPROCKET_PACKAGE_BREADTH_CM=20
SHIPROCKET_PACKAGE_HEIGHT_CM=5
SHIPROCKET_PER_ITEM_WEIGHT_KG=0.5
SHIPROCKET_MIN_WEIGHT_KG=0.5
# 6-digit PIN of your registered Shiprocket pickup address — used for serviceability checks
NEXT_PUBLIC_SHIPROCKET_PICKUP_PINCODE=110001

# Firebase Admin SDK (server-only). Download from Firebase console → Project settings
# → Service accounts → Generate new private key. Paste the WHOLE JSON on one line.
# Required for /api/orders/place (server-authoritative pricing), Cashfree verification,
# and admin notify-status route.
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","client_email":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"}

# Cashfree PG (server-only). Create test + live keys at merchant.cashfree.com →
# Developers → API Keys. Start with the sandbox environment; flip to production
# once KYC is approved.
# IMPORTANT: this must be the store OWNER's Cashfree account (merchant of record).
CASHFREE_APP_ID=TEST_APP_ID_xxxxxxxxxxxxxx
CASHFREE_SECRET_KEY=TEST_SECRET_xxxxxxxxxxxxxx
# One of: sandbox | production. Default is sandbox when unset.
CASHFREE_ENVIRONMENT=sandbox

# Per-order platform fee (bookkeeping only for now — recorded on the order doc
# for the admin earnings view). Set to 0 to disable. Cashfree Split Settlement
# is not wired yet; when it is, wire in CASHFREE_VENDOR_ID_DEVELOPER here.
PLATFORM_FEE_INR=20

# Resend transactional email. Sign up at resend.com, verify a domain you own,
# then set RESEND_FROM_ADDRESS to a verified sender on that domain.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_ADDRESS=SecondSkin <orders@secondskinmensworld.com>

# Google Gemini (server-only — never prefix with NEXT_PUBLIC_).
# Used by /api/admin/products/generate-image to turn a plain cloth photo into a
# photo of an Indian model wearing it (Gemini 2.5 Flash Image, "Nano Banana").
# Create a key at https://aistudio.google.com/apikey (requires a billed Google
# Cloud project — image generation is a paid feature).
# IMPORTANT: put the REAL key only in .env.local (gitignored), never here.
GEMINI_API_KEY=your-gemini-api-key
# Optional: override the image model. Default is gemini-2.5-flash-image (cheap,
# ~$0.04/image). For higher-quality results set gemini-3.1-flash-image.
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image

